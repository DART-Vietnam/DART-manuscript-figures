[project]
name = "dart-pipeline-manuscript"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "xarray[io]",
    "matplotlib",
    "cartopy>=0.25.0",
    "pillow>=11.3.0",
]
